name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 🔮 Horoscope gate
        uses: Andcool-Systems/deploy-horoscope-action@v1
        with:
          zodiac: virgo

      - name: 🚀 Deploy
        run: echo "Deploying to production..."